# Build intermediates
*.o
app/engine/*.o

# Built packages (distributed via GitHub releases)
*.eap

# macOS
.DS_Store

# Docker
.dockerignore

# Local Claude Code settings (may contain credentials)
.claude/settings.local.json
