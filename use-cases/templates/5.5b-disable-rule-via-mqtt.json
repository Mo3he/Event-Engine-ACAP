{
  "name": "Disable Rule via MQTT",
  "description": "Disable a specific rule when an MQTT command arrives. Useful for deactivating seasonal rules, switching operational profiles, or suppressing alert rules from a central control system. Pair with 5.5a to also be able to re-enable the same rule.",
  "enabled": true,
  "trigger_logic": "OR",
  "trigger_window": 0,
  "condition_logic": "AND",
  "cooldown": 0,
  "max_executions": 0,
  "max_exec_period": "",
  "triggers": [
    {
      "type": "mqtt_message",
      "topic_filter": "cameras/{{camera.serial}}/rules/YOUR_RULE_ID/enable",
      "payload_filter": "disable"
    }
  ],
  "conditions": [],
  "actions": [
    {
      "type": "set_rule_enabled",
      "rule_id": "REPLACE_WITH_TARGET_RULE_ID",
      "enabled": false
    },
    {
      "type": "send_syslog",
      "message": "Rule disabled via MQTT at {{timestamp}} on {{camera.model}} ({{camera.serial}})",
      "level": "info"
    }
  ]
}
